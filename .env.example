# Adzuna (https://developer.adzuna.com) — free signup required, no cost.
# Used by src/sources/apis/adzuna.ts. If unset, the Adzuna source is skipped
# (logged, not fatal) during `npm run crawl` — every other source still runs.
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
